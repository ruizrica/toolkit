# Open Questions

1. Should `agent-viewer render --stdin` become the generic universal path for all report families?
2. Should reports exports remain searchable client-side when fully standalone?
3. Should completion reports allow standalone rollback metadata or remain read-only outside live mode?
